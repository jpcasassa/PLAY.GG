server:
  port: 8080
spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      routes:
        - id: user-service
          uri: lb://user-service
          predicates: [ Path=/users/** ]
        - id: auth-service
          uri: lb://auth-service
          predicates: [ Path=/auth/** ]
        - id: profile-service
          uri: lb://profile-service
          predicates: [ Path=/profiles/** ]
        - id: forum-service
          uri: lb://forum-service
          predicates: [ Path=/posts/**, /comments/** ]
        - id: community-service
          uri: lb://community-service
          predicates: [ Path=/communities/** ]
        - id: chat-service
          uri: lb://chat-service
          predicates: [ Path=/messages/** ]
        - id: notification-service
          uri: lb://notification-service
          predicates: [ Path=/notifications/** ]
        - id: game-service
          uri: lb://game-service
          predicates: [ Path=/games/** ]
        - id: collectible-service
          uri: lb://collectible-service
          predicates: [ Path=/collectibles/** ]
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
logging:
  level:
    com.playgg.gateway: INFO